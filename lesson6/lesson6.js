class Weapon {
    constructor(title, attackLevel, strength, range) {
        this.title = title;
        this.attackLevel = attackLevel;
        this.strength = strength;
        this.range = range;
    }
}